# Source photography from the client. Kept on disk for re-cropping, never pushed:
# these are multi-megabyte originals and the repo only needs the derivatives.
assets/src/
assets/src2/

# Verification output, regenerated by tools/capture.mjs on demand.
tools/shots/
